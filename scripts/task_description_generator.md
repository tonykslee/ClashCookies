Run this first and read the output before responding:
./scripts/codex-task.sh

generate a detailed task description, wrapped in ``` ```, that goes into these 4 categories (GOAL, WHAT EXISTS TODAY, WHAT NEEDS TO CHANGE, SUCCESS CRITERIA) based on this unstructured blob. (note: I'll be feeding this back to you to implement in a separate chat without context):
